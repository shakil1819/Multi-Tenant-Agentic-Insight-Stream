{
  "name": "agentic-rag-chartjs",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit && npm run typecheck --prefix frontend",
    "test:changed": "node scripts/test-changed.mjs",
    "precommit": "npm run lint && npm run typecheck && npm run test:changed",
    "start": "node dist/src/server.js",
    "create-schema": "tsx scripts/create-schema.ts",
    "seed": "tsx scripts/seed.ts",
    "setup": "tsx scripts/wait-for-weaviate.ts && npm run create-schema && npm run seed",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "test": "vitest run",
    "prepare": "husky"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/swagger": "^9.7.0",
    "@fastify/swagger-ui": "^5.2.5",
    "@langchain/core": "^0.3.0",
    "@langchain/langgraph": "^0.2.0",
    "@langchain/openai": "^0.5.0",
    "dotenv": "^17.4.1",
    "fastify": "^5.8.4",
    "langchain": "^0.3.0",
    "langsmith": "^0.3.0",
    "weaviate-client": "^3.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^22.0.0",
    "eslint": "^9.39.4",
    "eslint-plugin-react-hooks": "^7.0.1",
    "globals": "^17.4.0",
    "husky": "^9.1.7",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.58.1",
    "vitest": "^3.0.0"
  }
}
